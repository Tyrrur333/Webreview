<%= stylesheet_link_tag "tops", :media => "all" %><main>  <div class="top-wrapper">    <div class="container">      <h1>Webサービスを見つけよう</h1>      <p>自分にあったWebサービスを</p>      <p>カテゴリや注目・人気、最近の投稿から見つけることができます</p>    </div>  </div>  <div class="container">    <div class="row">      <div class="col-md-9">        <h2>Webサービスを探す</h2>        <div class="find-category">          <p>カテゴリから探す</p>          <ul>            <li>              <% @category.each do |category|  %>                <%= link_to category.name, category_index_path(category.id) %><span> /</span>              <% end %>            </li>          </ul>        </div>        <div class="attention-posts">          <h2>注目・人気サイト</h2>          <div class="bxslider">            <% @appposts.each do |apppost|   %>              <!-- レビュー数取得-->              <% @review_count = apppost.reviews.count %>              <%# @attention_post = Apppost.all %>              <%# @attention_post = Apppost.joins(:reviews).order('@reviews desc') %>            <% end %>            <% @attention_post.each do |apppost|   %>              <div>                <%=link_to  (image_tag apppost.app_image.to_s) , apppost %>                <h2><%= link_to apppost.app_name, apppost %></h2>                <li>URL:<%= link_to apppost.url, apppost.url, target: "_blank" %></li>                <%= apppost.description %>              </div>            <% end %>          </div>        </div>      </div>      <div class="col-md-3">        <%= render 'shared/sidebar_recent_post' %>      </div>    </div>  </div></main><script>  $('.bxslider').bxSlider({      auto: true,      pose: 70,      autoControls: true,      stopAutoOnClick: true,      pager: true,      slideWidth: 425,      maxSlides: 2,      touchEnabled: true  });</script>